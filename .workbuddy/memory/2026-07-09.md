# 2026-07-09 工作日志

## 项目隔离确认
- 确认 D:\Chaniug-v2 是从老项目 Chaniug 原仓库复制而来的全新独立项目。
- 隔离约束（用户明确要求）：双向互不影响。
- 原则：后续所有改动仅限本目录，绝不触及老项目。

## 清理老项目引用并上传 GitHub
- 全局替换老项目**特有**引用（旧仓库 Chaniug/Chaniug、旧域名 valk.ccwu.cc、旧邮箱 cheniug99@gmail.com、QQ 1247903536、老本地路径 d:\mychaniug\Chaniug、项目标题名）→ 新仓库 chaniugv2 或中性占位符。
- 保留用户身份标识：GitHub 用户名 Chaniug、社交账号 valkjin、文案 Valkjin。
- 校验：全局搜索老项目特有串无残留（仅剩保留的 GitHub 用户名）。
- 提交 commit `62494cc`，创建并推送 public 仓库 `github.com/Chaniug/chaniugv2`（main 分支，origin 已配置并跟踪）。
- 待用户后续替换占位符（域名/邮箱/QQ/品牌/项目名），并决定 GitHub Pages 自定义域名（当前 CNAME 为占位，需改真实域名或删除）。

## 部署策略调整（同一会话后续）
- 用户决定：删除 CNAME 文件，GitHub Pages 改用默认域名 https://Chaniug.github.io/chaniugv2/。
- 将项目内全部 `your-domain.example.com` 占位（index.html / README.md / docs/PROJECT.md / package.json 共 13 处 + CNAME 1 处）统一替换为默认域名；CNAME 目录树说明同步更新为默认域名部署说明。
- 校验：全局搜索 `your-domain.example.com` 无残留（代码层已清零）。

## Phase A — Astro 脚手架 + CI（已提交 1997c35）
- Astro 5 重构静态站点；`astro.config.mjs` 设 `base:'/chaniugv2/'`、`outDir:dist`、`cssMinify:'lightningcss'`。
- 需显式安装 `lightningcss`（Astro 5 依赖）。
- `.github/workflows/deploy.yml` 替代 jekyll，权限 contents:read / pages:write / id-token:write。

## Phase B — 组件化 1:1 迁移（已提交 5b96e1d）
- 全部 HTML/CSS/JS 拆分为 BaseLayout + 13 组件 + src/data(site/skills/modals.json)。
- @tsparticles/slim 替代手写星空 canvas；vanilla-tilt / canvas-confetti / gsap 引入。
- 验证：build 通过，dist/index.html ~103KB，全区块在场。

## Phase C — 横向展示风格（已提交 36bc3b8）
- 新增 `css/modules/horizontal.css`：scroll-snap 横向滑轨（边缘渐隐 / 箭头 / 进度条 / 桌面拖动）。
- `RailNav.astro`：统一接入所有 `[data-rail]`（翻页/拖动/进度/禁用态）。
- Contact、Stats 的「最近项目」改为横向滑轨浏览。
- 新增 `Journey.astro` + `src/data/journey.ts`：横向时间轴模块，已加入导航。
- `ScrollNarrative.astro`：顶部渐变进度条 + GSAP 区块标题视差（ScrollTrigger 动态懒加载，reduced-motion 下仅原生进度条）。
- `Interactions.astro`：vanilla-tilt 3D 倾斜 + 玻璃卡鼠标光晕（此前漏放 body，本次补回并验证 build）。
- 约束坚守：未劫持整页横向滚动，仅区块内横向滑轨；移动端原生触摸滑动；reduced-motion 全程降级。
- 预览：astro preview 于 http://localhost:4321/chaniugv2/（HTTP 200 验证）。

## 待办（Phase D / E）
- Phase D：明暗主题切换 + 持久化；构建期拉取真实 GitHub 项目/计数器；本地轻量图表替代远程 SVG；GSAP 叙事深化。
- Phase E：Lighthouse 性能/SEO/a11y；移动端降级复核；清理旧 css/js/index.html；更新 README；最终 CI 部署验证。
- 占位符待替换：qq `your-qq`、email `you@example.com`、wechat 真实值。
