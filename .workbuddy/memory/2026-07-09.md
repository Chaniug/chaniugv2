# 2026-07-09 工作日志

## 项目隔离确认
- 确认 D:\Chaniug-v2 是从老项目 Chaniug 原仓库复制而来的全新独立项目。
- 隔离约束（用户明确要求）：双向互不影响。
- 原则：后续所有改动仅限本目录，绝不触及老项目。

## 清理老项目引用并上传 GitHub
- 全局替换老项目**特有**引用（旧仓库 Chaniug/Chaniug、旧域名 valk.ccwu.cc、旧邮箱 cheniug99@gmail.com、QQ 1247903536、老本地路径 d:\mychaniug\Chaniug、项目标题名）→ 新仓库 chaniugv2 或中性占位符。
- 保留用户身份标识：GitHub 用户名 Chaniug、社交账号 valkjin、文案 Valkjin。
- 校验：全局搜索老项目特有串无残留（仅剩保留的 GitHub 用户名）。
- 提交 commit `62494cc`，创建并推送 public 仓库 `github.com/Chaniug/chaniugv2`（main 分支，origin 已配置并跟踪）。
- 待用户后续替换占位符（域名/邮箱/QQ/品牌/项目名），并决定 GitHub Pages 自定义域名（当前 CNAME 为占位，需改真实域名或删除）。

## 部署策略调整（同一会话后续）
- 用户决定：删除 CNAME 文件，GitHub Pages 改用默认域名 https://Chaniug.github.io/chaniugv2/。
- 将项目内全部 `your-domain.example.com` 占位（index.html / README.md / docs/PROJECT.md / package.json 共 13 处 + CNAME 1 处）统一替换为默认域名；CNAME 目录树说明同步更新为默认域名部署说明。
- 校验：全局搜索 `your-domain.example.com` 无残留（代码层已清零）。
